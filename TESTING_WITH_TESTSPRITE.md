# 🛡️ Testing DeepEyeUnlocker Pro with TestSprite

This project is configured for **Autonomous Quality Assurance** using the TestSprite MCP Server. TestSprite monitors code changes, generates surgical test cases, and automatically proposes fixes for regressions.

## 📁 Repository Structure

- `/tests/`: Legacy and standard xUnit test suite.
- `/testsprite/`: Autonomous test generation landing zone.
  - `config.json`: Environment and runner configuration.
  - `/unit/`: Logic-level validations generated by AI.
  - `/integration/`: Cross-module workflow tests.

## 🚀 Running the Autonomous Loop

### 1. Discovery Phase

TestSprite scans the project intent.

```bash
# Run via IDE / Terminal
testsprite discover .
```

### 2. Generation Phase

Generate new tests for recently added Pro features (MTK/Qualcomm).

```bash
testsprite generate --focus "Protocols,ExpertCenter"
```

### 3. Execution & Auto-Debug

Run the suite and let TestSprite analyze failures in the `MTKExploitEngine`.

```bash
testsprite run --auto-fix --iterations 3
```

## 🛠️ Tech Stack & Coverage

- **Core**: 80% coverage target via `xUnit`.
- **UI**: State-machine testing for `MainViewModel`.
- **Safety**: Automated verification of "Expert Mode" gating.

---
*Maintained by the DeepEye Engineering Team.*
