<UserControl x:Class="DeepEye.UI.Modern.Views.UnlockCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource DeepEye.Text.Title}" Grid.Row="0"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel>
                <!-- MTK / BROM Operations -->
                <GroupBox Header="MTK [BROM / PRELOADER]" Foreground="{StaticResource DeepEye.Brush.Cyan}" Margin="10" Width="350">
                    <StackPanel Margin="10">
                        <Button Content="[BROM] ERASE FRP" Command="{Binding EraseFRPCommand}" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                        <Button Content="[BROM] FACTORY RESET" Command="{Binding FactoryResetCommand}" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                        <Button Content="[BROM] UNLOCK BOOTLOADER" Command="{Binding UnlockBootloaderCommand}" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                        <Button Content="[BROM] RELOCK BOOTLOADER" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                    </StackPanel>
                </GroupBox>

                <!-- Qualcomm / EDL Operations -->
                <GroupBox Header="QUALCOMM [EDL 9008]" Foreground="{StaticResource DeepEye.Brush.Orange}" Margin="10" Width="350">
                    <StackPanel Margin="10">
                        <Button Content="[EDL] ERASE FRP (GENERIC)" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                        <Button Content="[EDL] FACTORY RESET" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                        <Button Content="[EDL] REMOVE MI CLOUD" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                    </StackPanel>
                </GroupBox>

                <!-- Fastboot Operations -->
                <GroupBox Header="FASTBOOT / ADB" Foreground="{StaticResource DeepEye.Brush.White}" Margin="10" Width="350">
                    <StackPanel Margin="10">
                        <Button Content="[FB] ERASE USERDATA" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                        <Button Content="[ADB] REBOOT TO EDL" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                        <Button Content="[ADB] REBOOT TO BOOTLOADER" Style="{StaticResource DeepEye.Button.Modern}" Margin="5"/>
                    </StackPanel>
                </GroupBox>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
