<UserControl x:Class="DeepEye.UI.Modern.Views.FlashCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource DeepEye.Text.Title}" Grid.Row="0"/>

        <!-- File Selection Area -->
        <Border Grid.Row="1" Background="#0AFFFFFF" CornerRadius="8" Padding="15" Margin="0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="FIRMWARE / SCATTER FILE" Style="{StaticResource DeepEye.Text.Label}"/>
                    <TextBlock Text="{Binding FirmwarePath}" Foreground="White" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="BROWSE" Command="{Binding BrowseFirmwareCommand}" Style="{StaticResource DeepEye.Button.Modern}" Width="100"/>
            </Grid>
        </Border>

        <!-- Partition Grid (Placeholder) -->
        <Border Grid.Row="2" BorderBrush="#20FFFFFF" BorderThickness="1" Margin="0,10">
            <Grid>
                <TextBlock Text="Partition list will appear here after loading firmware..." 
                           Foreground="White" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Controls -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="START FLASH" Command="{Binding StartFlashCommand}" Style="{StaticResource DeepEye.Button.Modern}" Width="150" Height="40" FontSize="14" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>
