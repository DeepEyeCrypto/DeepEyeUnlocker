<UserControl x:Class="DeepEye.UI.Modern.Views.PartitionCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource DeepEye.Text.Title}" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
            <Button Content="READ PARTITIONS" Command="{Binding ScanPartitionsCommand}" 
                    Style="{StaticResource DeepEye.Button.Modern}" Width="150" Height="35"/>
            <TextBlock Text="⚠️ Proceed with caution. Destructive actions available here." 
                       Foreground="{StaticResource DeepEye.Brush.Orange}" 
                       VerticalAlignment="Center" Margin="20,0,0,0" Opacity="0.8" FontStyle="Italic"/>
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{Binding Partitions}" 
                  Background="#05FFFFFF" BorderThickness="0" Margin="0,10,0,0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="PARTITION NAME" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="START ADDRESS" DisplayMemberBinding="{Binding StartAddress}" Width="120"/>
                    <GridViewColumn Header="SIZE" DisplayMemberBinding="{Binding Size}" Width="100"/>
                    <GridViewColumn Header="ACTIONS" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="BACKUP" Command="{Binding BackupCommand}" 
                                            Style="{StaticResource DeepEye.Button.Modern}" Padding="8,2" FontSize="10" Margin="0,0,5,0"/>
                                    <Button Content="ERASE" Command="{Binding EraseCommand}" 
                                            Style="{StaticResource DeepEye.Button.Hazard}" Padding="8,2" FontSize="10"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
