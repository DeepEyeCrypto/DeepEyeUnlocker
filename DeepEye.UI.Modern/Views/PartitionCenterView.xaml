<UserControl x:Class="DeepEye.UI.Modern.Views.PartitionCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:DeepEye.UI.Modern.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Title}" Style="{StaticResource DeepEye.Text.Title}"/>
            <TextBlock Text="Cryptographically Secured Device Snapshots (AES-256-GCM)" Foreground="Gray" Margin="0,5,0,15"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#0AFFFFFF" CornerRadius="8" Padding="10">
            <Grid>
                <ListView ItemsSource="{Binding Partitions}" Background="Transparent" BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="PARTITION" DisplayMemberBinding="{Binding Name}" Width="150"/>
                            <GridViewColumn Header="SIZE (BYTES)" DisplayMemberBinding="{Binding SizeInBytes}" Width="150"/>
                            <GridViewColumn Header="LBA" DisplayMemberBinding="{Binding StartLba, StringFormat=0x{0:X8}}" Width="120"/>
                            <GridViewColumn Header="ACTIONS" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="BACKUP" 
                                                Command="{Binding DataContext.BackupPartitionCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource DeepEye.Button.Modern}"
                                                Height="30" FontSize="10" Padding="10,0"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Enumerating Device Partitions..." Foreground="{StaticResource DeepEye.Brush.Cyan}" FontSize="16"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="#151520" Margin="0,15,0,0" Padding="15" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="ENGINE STATUS" Foreground="Gray" FontSize="10"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
                <Button Grid.Column="1" Content="REFRESH TABLE" Command="{Binding RefreshPartitionsCommand}"
                        Style="{StaticResource DeepEye.Button.Modern}" Height="40" Padding="20,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
