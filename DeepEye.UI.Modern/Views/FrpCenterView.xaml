<UserControl x:Class="DeepEye.UI.Modern.Views.FrpCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource DeepEye.Text.Title}" Grid.Row="0"/>

        <Border Grid.Row="1" Background="#0AFFFFFF" CornerRadius="8" Padding="15" Margin="0,15">
            <StackPanel>
                <TextBlock Text="SELECT TARGET BRAND / OS PROFILE" Foreground="Gray" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                <ComboBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}"
                          DisplayMemberPath="Brand"
                          Height="40" Background="#151520" Foreground="White" BorderThickness="1" BorderBrush="#333"/>
                
                <Grid Margin="0,15,0,0" x:Name="ProfileDetails" Visibility="{Binding SelectedProfile, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="CHIPSET" Foreground="Gray" FontSize="10"/>
                        <TextBlock Text="{Binding SelectedProfile.ChipsetFamily}" Foreground="{StaticResource DeepEye.Brush.Cyan}" FontSize="16" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="METHOD" Foreground="Gray" FontSize="10"/>
                        <TextBlock Text="{Binding SelectedProfile.Method}" Foreground="{StaticResource DeepEye.Brush.Orange}" FontSize="16" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Border Background="#05FFFFFF" CornerRadius="8" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="2026 BYPASS STRATEGY" Foreground="Gray" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding SelectedProfile.BypassSteps}">
                            <!-- This is simplified for the demo -->
                            <TextBlock Text="• Initializing Protocol Handshake..." Foreground="#AAFFFFFF" Margin="0,5"/>
                            <TextBlock Text="• Loading Specialized 2026 DA/Programmer..." Foreground="#AAFFFFFF" Margin="0,5"/>
                            <TextBlock Text="• Executing Partition Write Override..." Foreground="#AAFFFFFF" Margin="0,5"/>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <Button Content="START AUTOMATED BYPASS" Command="{Binding StartBypassCommand}" 
                    Style="{StaticResource DeepEye.Button.Modern}" Height="50"
                    IsEnabled="{Binding IsBypassing, Converter={StaticResource InverseBooleanConverter}}"/>
            <ProgressBar IsIndeterminate="{Binding IsBypassing}" Height="4" Margin="0,10,0,0" 
                         Visibility="{Binding IsBypassing, Converter={StaticResource BooleanToVisibilityConverter}}"
                         Foreground="{StaticResource DeepEye.Brush.Cyan}"/>
        </StackPanel>
    </Grid>
</UserControl>
