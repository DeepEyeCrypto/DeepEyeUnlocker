<UserControl x:Class="DeepEye.UI.Modern.Views.DeviceInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource DeepEye.Text.Title}" Grid.Row="0"/>

        <ScrollViewer Grid.Row="1" Margin="0,20,0,0">
            <StackPanel>
                <!-- Identity Card -->
                <Border Background="#0AFFFFFF" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="80" Height="80" Background="#1000F2FF" CornerRadius="40" VerticalAlignment="Top">
                            <TextBlock Text="📱" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="20,0,0,0">
                            <TextBlock Text="{Binding DeviceName}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                            <TextBlock Text="{Binding ChipsetInfo}" Style="{StaticResource DeepEye.Text.Label}" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding ConnectionStatus}" Foreground="{StaticResource DeepEye.Brush.Cyan}" FontSize="12" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Detailed Specs Grid -->
                <UniformGrid Columns="2">
                    <Border Background="#05FFFFFF" Padding="15" Margin="5" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="HARDWARE" Style="{StaticResource DeepEye.Text.Label}" Opacity="0.5"/>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="CPU/SoC:" Foreground="#A0FFFFFF" Margin="0,2"/>
                                    <TextBlock Text="Manufacturer:" Foreground="#A0FFFFFF" Margin="0,2"/>
                                    <TextBlock Text="Serial:" Foreground="#A0FFFFFF" Margin="0,2"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="20,0,0,0">
                                    <TextBlock Text="{Binding SoC}" Foreground="White" Margin="0,2"/>
                                    <TextBlock Text="{Binding Brand}" Foreground="White" Margin="0,2"/>
                                    <TextBlock Text="{Binding Serial}" Foreground="White" Margin="0,2"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Background="#05FFFFFF" Padding="15" Margin="5" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="SOFTWARE" Style="{StaticResource DeepEye.Text.Label}" Opacity="0.5"/>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Android Ver:" Foreground="#A0FFFFFF" Margin="0,2"/>
                                    <TextBlock Text="Patch Level:" Foreground="#A0FFFFFF" Margin="0,2"/>
                                    <TextBlock Text="Build ID:" Foreground="#A0FFFFFF" Margin="0,2"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="20,0,0,0">
                                    <TextBlock Text="14.0" Foreground="White" Margin="0,2"/>
                                    <TextBlock Text="2024-01-05" Foreground="White" Margin="0,2"/>
                                    <TextBlock Text="UP1A.231005.007" Foreground="White" Margin="0,2"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <Button Content="GENERATE FULL DIAGNOSTIC REPORT" 
                        Style="{StaticResource DeepEye.Button.Modern}" 
                        Margin="5,20,5,0" Height="45" FontWeight="Bold"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
