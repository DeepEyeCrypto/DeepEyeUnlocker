#!/bin/bash
# [INFERRED] Delegates commit gating to the repository-wide DeepEye v7 verification suite.

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

./deepeye-test.sh
