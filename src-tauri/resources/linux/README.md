# Linux bundled tools

Place Linux executables used by runtime commands in this directory.

Expected filenames:

- `adb`
- `irecovery`
- `ideviceinfo`
- `idevicepair`
- `idevicerestore`
- `iproxy` (optional)

These files are bundled via `resources/linux/*` in `tauri.conf.json`.
