

# Fast type check
npx tsc --noEmit --skipLibCheck

# Lint staged files only (fast)
npx lint-staged

STAGED_TS_FILES=$(git diff --cached --name-only | grep -E '\.(ts|tsx)$' || true)
if [ -n "$STAGED_TS_FILES" ]; then
  npx jest --passWithNoTests --findRelatedTests $STAGED_TS_FILES
fi
