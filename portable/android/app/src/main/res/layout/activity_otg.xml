<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212">

    <!-- MAIN CONTENT -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Top Header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="#1E1E22"
            android:gravity="center_vertical"
            android:paddingHorizontal="16dp"
            android:elevation="4dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="DEEP"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:textStyle="bold" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="EYE"
                android:textColor="#00F2FF"
                android:textSize="18sp"
                android:textStyle="bold" />
            
            <View android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1"/>
            
            <TextView
                android:id="@+id/connectionIndicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="● OFFLINE"
                android:textColor="#FF5252"
                android:textSize="12sp"
                android:textStyle="bold"/>
        </LinearLayout>

        <!-- Brand Tabs (Horizontal Scroll) -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:scrollbars="none"
            android:background="#252528">
            <LinearLayout
                android:id="@+id/brandTabs1"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:paddingHorizontal="8dp"
                android:gravity="center_vertical">
                <!-- Populated programmatically or existing IDs -->
                <TextView android:id="@+id/brandXiaomi" style="@style/BrandTab" android:text="Xiaomi" android:background="@drawable/brand_tab_selected"/>
                <TextView android:id="@+id/brandSamsung" style="@style/BrandTab" android:text="Samsung"/>
                <TextView android:id="@+id/brandOppo" style="@style/BrandTab" android:text="OPPO"/>
                <TextView android:id="@+id/brandVivo" style="@style/BrandTab" android:text="Vivo"/>
                <TextView android:id="@+id/brandRealme" style="@style/BrandTab" android:text="Realme"/>
                <TextView android:id="@+id/brandHuawei" style="@style/BrandTab" android:text="Huawei"/>
                <TextView android:id="@+id/brandOnePlus" style="@style/BrandTab" android:text="OnePlus"/>
            </LinearLayout>
        </HorizontalScrollView>

        <!-- Model Select & Mode Bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="12dp"
            android:background="#1A1A1E">

            <!-- Model Selector Button -->
            <Button
                android:id="@+id/btnSelectModel"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="📱 SELECT MODEL"
                android:textColor="#00F2FF"
                android:backgroundTint="#2C2C30"
                android:drawableEnd="@android:drawable/arrow_down_float"
                android:drawableTint="#00F2FF"
                android:paddingHorizontal="16dp"/>

            <!-- Remote Button -->
            <Button
                android:id="@+id/btnRemoteUnlock"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="📡 REMOTE"
                android:textColor="#FFFFFF"
                android:backgroundTint="#6200EA"
                android:layout_marginStart="8dp"/>
        </LinearLayout>

        <!-- Operations List (Full Width) -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:padding="12dp"
            android:clipToPadding="false">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="UNLOCK OPERATIONS"
                    android:textColor="#00F2FF"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:layout_marginBottom="8dp"/>
                <Button android:id="@+id/btnUnlockBl" style="@style/OperationButton" android:text="UNLOCK BOOTLOADER"/>
                <Button android:id="@+id/btnRelockBl" style="@style/OperationButton" android:text="RELOCK BOOTLOADER"/>
                <Button android:id="@+id/btnEraseFrp" style="@style/OperationButtonOrange" android:text="ERASE FRP LOCK"/>
                <Button android:id="@+id/btnFactoryReset" style="@style/OperationButtonDanger" android:text="FACTORY RESET (USERDATA)"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="SECURITY REPAIR"
                    android:textColor="#FF6B35"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:layout_marginTop="16dp"
                    android:layout_marginBottom="8dp"/>
                <Button android:id="@+id/btnRemovePin" style="@style/OperationButton" android:text="REMOVE SCREEN LOCK (SAFE)"/>
                <Button android:id="@+id/btnRemoveMiAccount" style="@style/OperationButton" android:text="REMOVE MI CLOUD"/>
                <Button android:id="@+id/btnBypassAuth" style="@style/OperationButton" android:text="AUTH BYPASS (SLA/DA)"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="DIAGNOSTICS"
                    android:textColor="#00E676"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:layout_marginTop="16dp"
                    android:layout_marginBottom="8dp"/>
                <Button android:id="@+id/btnReadInfo" style="@style/OperationButton" android:text="READ DEVICE INFO"/>
                <Button android:id="@+id/btnReadImei" style="@style/OperationButton" android:text="READ IMEI / NVRAM"/>
                
                <!-- Added spacing for bottom -->
                <View android:layout_width="match_parent" android:layout_height="80dp"/>
            </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <!-- LOG OVERLAY (Freya Style Popup) -->
    <RelativeLayout
        android:id="@+id/logOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#CC000000"
        android:visibility="gone"
        android:elevation="20dp"
        android:clickable="true"
        android:focusable="true">

        <!-- Console Window -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="400dp"
            android:layout_alignParentBottom="true"
            android:background="@drawable/panel_bg"
            android:orientation="vertical">

            <!-- Title Bar -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:background="#222222"
                android:paddingHorizontal="16dp">
                
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="PROCESS LOG"
                    android:textColor="#FFFFFF"
                    android:textStyle="bold"
                    android:layout_centerVertical="true"/>

                <ImageView
                    android:id="@+id/btnCloseLog"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@android:drawable/ic_menu_close_clear_cancel"
                    android:tint="#FF5252"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"/>
            </RelativeLayout>

            <!-- Scrollable Terminal -->
            <ScrollView
                android:id="@+id/logScrollView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#000000"
                android:padding="12dp"
                android:fillViewport="true">

                <TextView
                    android:id="@+id/terminalText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="> Initializing...\n> Waiting for device..."
                    android:textColor="#00E676"
                    android:fontFamily="monospace"
                    android:textSize="12sp"
                    android:lineSpacingExtra="4dp"/>
            </ScrollView>
        </LinearLayout>
    </RelativeLayout>

</FrameLayout>
